name: Test

on:
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest

    steps:
    - name: Run tests
      env:
        SHELL: "zsh"
      run: wget -qO- https://get.pnpm.io/install.sh | sh - && pnpm run test
